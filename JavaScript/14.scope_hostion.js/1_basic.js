/**
 * scope(스코프 : 범위) : 메모리 관리, 이름 충돌 방지, 블록 처리 단위 개념
 * block(블록 : {})
 * { 블록 단위 실행 : 실행문(for, switch~case, while...) }
 */
{
    //전역변수, Global variable
    //파일전체 이느곳에서든 호출 가능
    //변수의 초기화는 자동
    let number;
    
}